name: lottery_app
description: A new Flutter application.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0

  #Fonts:
  google_fonts: ^2.0.0
  font_awesome_flutter: ^9.1.0

  #FireBase:
  firebase_core: ^1.0.4
  firebase_auth: ^3.1.1
  cloud_firestore: ^2.5.1


  #State Management:
  provider: ^6.0.0

  #Login
  flutter_signin_button: ^2.0.0
  #flutter_auth_buttons: ^0.10.0
  sign_in_with_apple: ^3.0.0
  google_sign_in: ^5.0.2

  #flutter_facebook_auth: ^0.3.3
  animations: ^2.0.1

  #To hold quick DB located on the mobile (for Registered flag)
  shared_preferences: any

  #dependecy injection:
  get_it: ^7.2.0

  #to recognize if the server response OK or not (Using Either for example)
  dartz: ^0.10.0

  #PDF Package for future use
  pdf_render: ^1.0.12

  #A nice way to control all buttons/textfields
  flutter_form_builder: any

  # for verification code fields _ _ _ _
  flutter_verification_code: ^1.0.1
  pin_code_fields: ^7.0.0

  #For displaying alert dialog for few minutes
  flash: ^2.0.1
  #flushbar: ^1.10.4

  #RTL for Hebrew LTR for English
  auto_direction: ^0.0.5

  flutter_progress_button: ^1.0.0

  xml: ^5.1.1
  rive: ^0.7.28

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
     - covid.pdf
  #   - images/a_dot_ham.jpeg
     - new_file.riv
     - new_animation.riv
     - new_animation1.riv
